{
  "name": "golazo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node scripts/test-engines.mjs",
    "dev:emulator": "VITE_FB_EMULATOR=1 vite",
    "build:emulator": "VITE_FB_EMULATOR=1 vite build",
    "emulators": "firebase emulators:start --only auth,firestore --project demo-over-wc26",
    "seed": "node scripts/seed.mjs",
    "deploy:rules": "firebase deploy --only firestore:rules",
    "deploy:hosting": "npm run build && firebase deploy --only hosting"
  },
  "dependencies": {
    "firebase": "^10.12.2",
    "qrcode": "^1.5.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "autoprefixer": "^10.4.19",
    "firebase-admin": "^12.1.0",
    "postcss": "^8.4.38",
    "puppeteer-core": "^25.1.0",
    "tailwindcss": "^3.4.4",
    "vite": "^5.3.1"
  }
}
